const gameBoard = (() => {
  const board = ["X", "O", "X", "X", "O", "O", "X", "O", "X"];

  return { board };
})();

const Player = (symbol) => {
  return { symbol };
};

const gameController = (() => {})();

const displayController = (() => {})();
